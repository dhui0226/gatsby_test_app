import * as React from "react"
import Layout from "../components/Layout"

const AboutPage = () => {
    return (
        <Layout pageTitle="About Page">
            <p>Hi there! I built this with Gatsby.</p>
        </Layout>
    )
}

export default AboutPage